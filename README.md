# MXA_experiment

## Setup
1. Installeer Python en pip
2. Installeer dependencies: `pip install ollama python-dotenv openai`
3. Plaats je OpenAI key in `.env`
4. Zet `USE_LOCAL = True` in `config.py` om lokaal te testen

## Run
```bash
python mxa.py